# Supabase Configuration
# Copy this file to .env and fill in your Supabase project details

# Supabase Project URL (从 Supabase Dashboard 获取)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Service Role Key (从 Supabase Dashboard > Settings > API 获取)
# ⚠️ 警告: Service Role Key 拥有完全权限，仅在后端使用，不要暴露给前端
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase Anon Key (可选，用于公开访问)
SUPABASE_ANON_KEY=your-anon-key-here

# 数据库配置 (如果使用 Supabase 数据库)
SIAPP_DATABASE_TYPE=postgres
SIAPP_DB_HOST=db.your-project.supabase.co
SIAPP_DB_PORT=5432
SIAPP_DB_USER=postgres
SIAPP_DB_PASSWORD=your-database-password
SIAPP_DB_NAME=postgres
SIAPP_DB_SSLMODE=require

# 应用配置
SIAPP_ADDR=0.0.0.0:8080

# CORS 配置
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 注意事项:
# 1. Supabase JWT 验证需要能够访问 SUPABASE_URL/auth/v1/jwks
# 2. Service Role Key 仅用于后端管理操作，不要在前端代码中使用
# 3. 前端应该使用 Anon Key 和 Supabase JS 客户端
# 4. 确保数据库连接信息正确，特别是 SSL 模式